import 'package:flutter/material.dart';

class HomeTab {
  final String title;
  final IconData icon;
  final MaterialColor color;
  final Key btnKey;
  final Key titleKey;
  const HomeTab(this.title, this.icon, this.color, this.btnKey, this.titleKey);
}
