import 'package:flutter/material.dart';

class AddCardScreenUnkownErrorContent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Unknown Error'),
      ),
    );
  }
}