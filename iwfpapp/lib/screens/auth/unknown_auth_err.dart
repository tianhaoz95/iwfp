import 'package:flutter/material.dart';

class UnknownAuthErrorContent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Unkown auth error'),
    );
  }
}
