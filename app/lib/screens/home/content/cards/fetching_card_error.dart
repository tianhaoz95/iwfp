import 'package:flutter/material.dart';

class FetchingCardError extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: ListView(
        children: [
          Text('Error Fetching Cards')
        ],
      ),
    );
  }
}